apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  labels:
    app.kubernetes.io/name: cpk
    app.kubernetes.io/version: 5.7.5
  name: postgresstigassessments.security.crunchydata.com
spec:
  group: security.crunchydata.com
  names:
    kind: PostgresSTIGAssessment
    listKind: PostgresSTIGAssessmentList
    plural: postgresstigassessments
    singular: postgresstigassessment
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.summary.pass
          name: Pass
          type: integer
        - jsonPath: .status.summary.fail
          name: Fail
          type: integer
        - jsonPath: .status.summary.skip
          name: Skip
          type: integer
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: PostgresSTIGAssessment is the Schema for the postgresstigassessments API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: PostgresSTIGAssessmentSpec defines the desired state of PostgresSTIGAssessment
              properties:
                connection:
                  description: |-
                    Connection defines the information necessary to connect
                    to PostgreSQL and run the STIG assessment.
                  properties:
                    dbname:
                      default: postgres
                      type: string
                    host:
                      minLength: 1
                      type: string
                    passwordSecretRef:
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                        - key
                        - name
                      type: object
                    port:
                      default: 5432
                      format: int32
                      type: integer
                    user:
                      default: postgres
                      type: string
                  required:
                    - host
                    - passwordSecretRef
                  type: object
                image:
                  description: The image name to use for the Crunchy Postgres STIG Playbook job.
                  type: string
                imagePullPolicy:
                  description: |-
                    ImagePullPolicy is used to determine when Kubernetes will attempt to
                    pull (download) container images.
                    More info: https://kubernetes.io/docs/concepts/containers/images/#image-pull-policy
                  enum:
                    - Always
                    - Never
                    - IfNotPresent
                  type: string
                imagePullSecrets:
                  description: |-
                    The image pull secrets used to pull from a private registry.
                    https://k8s.io/docs/tasks/configure-pod-container/pull-image-private-registry/
                  items:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          TODO: Add other useful fields. apiVersion, kind, uid?
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  type: array
                metadata:
                  description: Metadata contains labels and annotations
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      type: object
                    labels:
                      additionalProperties:
                        type: string
                      type: object
                  type: object
                overrides:
                  description: List of control overrides. A control cannot be listed more than once.
                  items:
                    properties:
                      control:
                        description: Control name
                        minLength: 1
                        type: string
                      message:
                        description: Message that justifies a status override
                        type: string
                      status:
                        description: Forced status for the control
                        enum:
                          - FAILED
                          - PASSED
                          - SKIPPED
                        type: string
                    required:
                      - control
                      - message
                      - status
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - control
                  x-kubernetes-list-type: map
                variables:
                  description: |-
                    Variables that allow you to define configuration needed by the STIG assessment.
                    Some variables will be overwritten by configuration required by the Operator
                    or other values from the spec (e.g., connection information or overrides)
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
              required:
                - connection
              type: object
            status:
              description: PostgresSTIGAssessmentStatus defines the observed state of PostgresSTIGAssessment
              properties:
                conditions:
                  description: conditions represent the observations of PostgresSTIGAssessment's current state.
                  items:
                    description: "Condition contains details for one aspect of the current state of this API Resource.\n---\nThis struct is intended for direct use as an array at the field path .status.conditions.  For example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the observations of a foo's current state.\n\t    // Known .status.conditions.type are: \"Available\", \"Progressing\", and\
                      \ \"Degraded\"\n\t    // +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t    // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t    // other fields\n\t}"
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: |-
                          type of condition in CamelCase or in foo.example.com/CamelCase.
                          ---
                          Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                          useful (see .node.status.conditions), the ability to deconflict is important.
                          The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                observedGeneration:
                  description: observedGeneration represents the .metadata.generation on which the status was based.
                  format: int64
                  minimum: 0
                  type: integer
                summary:
                  description: ReportSummary provides a status count summary
                  properties:
                    fail:
                      type: integer
                    pass:
                      type: integer
                    skip:
                      type: integer
                  type: object
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
